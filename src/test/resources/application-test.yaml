ktor:
    application:
        modules:
            - com.ApplicationKt.module
    deployment:
        port: 8080

mongodb:
    # Use a test database or embedded MongoDB for tests
    uri: "mongodb://localhost:27017"
    database: "one_piece_timeline_test"
