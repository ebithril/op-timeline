ktor:
    application:
        modules:
            - com.ApplicationKt.module
    deployment:
        port: ${PORT:8080}

mongodb:
    uri: ${MONGODB_URI:"mongodb://localhost:27017"}
    database: "one_piece_timeline"
